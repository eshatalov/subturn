// core/limits.ts — caller-visible bounds, kept apart from core.ts so the
// interface table can quote them without loading the core.

/** await: default and maximum wait, seconds. */
export const MAX_WAIT_S = 300;
